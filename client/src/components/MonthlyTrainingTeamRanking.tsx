import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { useCampaign } from "@/contexts/CampaignContext";
import { trpc } from "@/lib/trpc";
import { Goal, Medal, Save, Trophy } from "lucide-react";
import { useMemo, useState } from "react";
import { toast } from "sonner";

const todayMonth = () => new Date().toISOString().slice(0, 7);

export function MonthlyTrainingTeamRanking() {
  const { activeCampaign } = useCampaign(); const utils = trpc.useUtils(); const [month, setMonth] = useState(todayMonth); const [drafts, setDrafts] = useState<Record<number, string>>({});
  const ranking = trpc.volunteers.training.monthlyRanking.useQuery({ campaignId: activeCampaign!.id, month });
  const setGoal = trpc.volunteers.training.setMonthlyGoal.useMutation({ onSuccess: async () => { await utils.volunteers.training.monthlyRanking.invalidate(); setDrafts({}); toast.success("Meta mensal atualizada."); }, onError: error => toast.error(error.message) });
  const totals = useMemo(() => (ranking.data ?? []).reduce((acc, item) => ({ completed: acc.completed + item.completedTrainingsThisMonth, target: acc.target + item.targetCompletions }), { completed: 0, target: 0 }), [ranking.data]);
  if (ranking.isLoading) return <div className="mt-6 h-72 animate-pulse rounded-2xl bg-muted" />;
  if (ranking.isError) return <section className="mt-6 rounded-2xl border border-rose-200 bg-rose-50 p-5" role="alert"><h2 className="font-serif text-xl text-rose-950">Não foi possível carregar o ranking mensal</h2><p className="mt-1 text-sm text-rose-800">Tente novamente para consultar metas e conclusões integrais da trilha.</p><Button className="mt-4" variant="outline" onClick={() => ranking.refetch()}>Tentar novamente</Button></section>;
  return <section className="mt-6 rounded-2xl border bg-card p-5 shadow-sm"><div className="flex flex-col justify-between gap-4 lg:flex-row lg:items-start"><div className="flex gap-3"><span className="flex size-10 shrink-0 items-center justify-center rounded-xl bg-[#103527] text-white"><Trophy className="size-5" /></span><div><h2 className="font-serif text-2xl">Ranking mensal de formação</h2><p className="mt-1 text-sm text-muted-foreground">Comparativo por equipe responsável, baseado em voluntários que concluíram integralmente a trilha no mês.</p></div></div><label className="text-xs font-semibold text-muted-foreground">Mês de referência<Input aria-label="Mês de referência do ranking" className="mt-1" type="month" value={month} onChange={event => setMonth(event.target.value || todayMonth())} /></label></div><div className="mt-5 grid gap-3 sm:grid-cols-3"><div className="rounded-xl bg-muted/50 p-4"><p className="text-xs font-bold uppercase tracking-wide text-muted-foreground">Equipes no ranking</p><p className="mt-1 font-serif text-3xl">{ranking.data?.length ?? 0}</p></div><div className="rounded-xl bg-muted/50 p-4"><p className="text-xs font-bold uppercase tracking-wide text-muted-foreground">Trilhas concluídas no mês</p><p className="mt-1 font-serif text-3xl">{totals.completed}</p></div><div className="rounded-xl bg-muted/50 p-4"><p className="text-xs font-bold uppercase tracking-wide text-muted-foreground">Meta consolidada</p><p className="mt-1 font-serif text-3xl">{totals.target}</p></div></div><div className="mt-6 space-y-3">{(ranking.data ?? []).map((item, index) => { const targetValue = drafts[item.coordinatorMemberId] ?? String(item.targetCompletions); return <article key={item.coordinatorMemberId} className="rounded-xl border p-4"><div className="flex flex-col gap-4 lg:flex-row lg:items-center"><div className="flex min-w-0 flex-1 items-start gap-3"><span className={`flex size-9 shrink-0 items-center justify-center rounded-full text-sm font-bold ${index === 0 ? "bg-[#c9a85b] text-[#103527]" : "bg-muted text-primary"}`}>{index === 0 ? <Medal className="size-5" /> : index + 1}</span><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><h3 className="truncate font-medium">Equipe de {item.name}</h3>{item.region && <Badge variant="outline">{item.region}</Badge>}</div><p className="mt-1 text-xs text-muted-foreground">{item.assignedVolunteers} voluntário(s) vinculado(s) · {item.completedTrainingsThisMonth} trilha(s) concluída(s) no mês</p></div></div><div className="w-full lg:w-56"><div className="flex justify-between text-xs font-medium text-muted-foreground"><span>Avanço da meta</span><span>{item.hasGoal ? `${item.goalProgress}%` : "Sem meta"}</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-muted"><div className="h-full bg-[#c9a85b]" style={{ width: `${item.goalProgress}%` }} /></div></div><div className="flex items-end gap-2"><label className="min-w-24 text-xs font-semibold text-muted-foreground">Meta do mês<Input aria-label={`Meta da equipe de ${item.name}`} className="mt-1" type="number" min="0" value={targetValue} onChange={event => setDrafts({ ...drafts, [item.coordinatorMemberId]: event.target.value })} /></label><Button size="sm" variant="outline" disabled={setGoal.isPending || Number(targetValue) === item.targetCompletions} onClick={() => setGoal.mutate({ campaignId: activeCampaign!.id, coordinatorMemberId: item.coordinatorMemberId, month, targetCompletions: Math.max(0, Number(targetValue) || 0) })}><Save className="mr-1 size-3" />Salvar</Button></div></div></article>; })}{!(ranking.data ?? []).length && <div className="rounded-xl border border-dashed bg-muted/30 p-6 text-center"><Goal className="mx-auto size-5 text-primary" /><p className="mt-2 text-sm font-medium">Ainda não há equipes com voluntários vinculados</p><p className="mt-1 text-xs text-muted-foreground">Associe voluntários a responsáveis de equipe para iniciar o ranking mensal.</p></div>}</div></section>;
}
