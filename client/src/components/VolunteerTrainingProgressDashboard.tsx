import { Badge } from "@/components/ui/badge";
import { useCampaign } from "@/contexts/CampaignContext";
import { trpc } from "@/lib/trpc";
import { BarChart3, Download, FileSpreadsheet, TimerReset, UsersRound } from "lucide-react";
import { Button } from "@/components/ui/button";
import { jsPDF } from "jspdf";
import { useMemo, useState } from "react";

function Metric({ label, value, description }: { label: string; value: string | number; description: string }) { return <article className="rounded-xl border bg-background/60 p-4"><p className="text-xs font-bold uppercase tracking-wide text-muted-foreground">{label}</p><p className="mt-2 font-serif text-3xl">{value}</p><p className="mt-1 text-xs text-muted-foreground">{description}</p></article>; }

export function VolunteerTrainingProgressDashboard() {
  const { activeCampaign } = useCampaign(); const [memberFilter, setMemberFilter] = useState(""); const [regionFilter, setRegionFilter] = useState("");
  const members = trpc.team.list.useQuery({ campaignId: activeCampaign!.id }); const volunteers = trpc.volunteers.list.useQuery({ campaignId: activeCampaign!.id });
  const dashboard = trpc.volunteers.training.dashboard.useQuery({ campaignId: activeCampaign!.id, coordinatorMemberId: memberFilter ? Number(memberFilter) : undefined, region: regionFilter || undefined });
  const regions = useMemo(() => Array.from(new Set((volunteers.data ?? []).map(item => item.region).filter((item): item is string => Boolean(item)))).sort(), [volunteers.data]);
  const exportCsv = () => { if (!dashboard.data) return; const rows = [["Voluntário", "Responsável", "Região", "Progresso", "Materiais concluídos", "Prazos vencidos"], ...dashboard.data.volunteers.map(item => [item.name, item.coordinatorName ?? "Sem responsável", item.region ?? "Sem região", `${item.progress}%`, `${item.completed}/${item.total}`, String(item.overdue)])]; const csv = rows.map(row => row.map(value => `"${String(value).replaceAll('"', '""')}"`).join(";")).join("\n"); const url = URL.createObjectURL(new Blob(["\uFEFF", csv], { type: "text/csv;charset=utf-8" })); const link = document.createElement("a"); link.href = url; link.download = "progresso-formacao-voluntarios.csv"; link.click(); URL.revokeObjectURL(url); };
  const exportPdf = () => { if (!dashboard.data) return; const data = dashboard.data; const pdf = new jsPDF({ unit: "mm", format: "a4" }); pdf.setFillColor(16, 53, 39); pdf.rect(0, 0, 210, 31, "F"); pdf.setTextColor(255, 253, 248); pdf.setFont("times", "bold"); pdf.setFontSize(20); pdf.text("Progresso da formação", 15, 18); pdf.setFont("helvetica", "normal"); pdf.setFontSize(9); pdf.text(activeCampaign!.name, 15, 25); pdf.setTextColor(28, 36, 31); pdf.setFont("helvetica", "bold"); pdf.setFontSize(12); pdf.text(`Taxa de conclusão: ${data.summary.completionRate}%`, 15, 44); pdf.text(`Voluntários: ${data.summary.volunteers}`, 15, 52); pdf.text(`Com vencimento: ${data.summary.overdueVolunteers}`, 85, 52); let y = 67; pdf.setFontSize(10); pdf.text("Voluntário", 15, y); pdf.text("Responsável", 71, y); pdf.text("Região", 120, y); pdf.text("Progresso", 156, y); pdf.text("Vencidos", 183, y); y += 5; pdf.setDrawColor(210, 213, 210); pdf.line(15, y, 195, y); y += 6; pdf.setFont("helvetica", "normal"); pdf.setFontSize(8); data.volunteers.forEach(item => { if (y > 278) { pdf.addPage(); y = 22; } pdf.text(pdf.splitTextToSize(item.name, 50), 15, y); pdf.text(pdf.splitTextToSize(item.coordinatorName ?? "Sem responsável", 43), 71, y); pdf.text(pdf.splitTextToSize(item.region ?? "Sem região", 30), 120, y); pdf.text(`${item.progress}%`, 158, y); pdf.text(String(item.overdue), 186, y); y += 9; }); pdf.setFontSize(8); pdf.setTextColor(95, 102, 98); pdf.text(`Gerado em ${new Date().toLocaleString("pt-BR")}`, 15, 289); pdf.save("progresso-formacao-voluntarios.pdf"); };
  if (dashboard.isLoading || members.isLoading || volunteers.isLoading) return <div className="mt-6 h-72 animate-pulse rounded-2xl bg-muted" />;
  if (dashboard.isError) return null;
  const data = dashboard.data; if (!data) return null;
  return <section className="mt-6 rounded-2xl border bg-card p-5 shadow-sm"><div className="flex flex-col justify-between gap-4 lg:flex-row lg:items-start"><div className="flex gap-3"><span className="flex size-10 shrink-0 items-center justify-center rounded-xl bg-[#103527] text-white"><BarChart3 className="size-5" /></span><div><h2 className="font-serif text-2xl">Progresso da formação</h2><p className="mt-1 text-sm text-muted-foreground">Visão agregada de avanço e vencimentos. Nenhum dado de contato é exibido neste painel.</p></div></div><div className="grid gap-2 sm:grid-cols-2"><label className="text-xs font-semibold text-muted-foreground">Equipe responsável<select aria-label="Filtrar por equipe responsável" className="mt-1 h-10 w-full rounded-md border bg-background px-3 text-sm text-foreground" value={memberFilter} onChange={event => setMemberFilter(event.target.value)}><option value="">Todas as equipes</option>{(members.data ?? []).filter(member => member.active).map(member => <option key={member.id} value={member.id}>{member.name}</option>)}</select></label><label className="text-xs font-semibold text-muted-foreground">Região<select aria-label="Filtrar por região" className="mt-1 h-10 w-full rounded-md border bg-background px-3 text-sm text-foreground" value={regionFilter} onChange={event => setRegionFilter(event.target.value)}><option value="">Todas as regiões</option>{regions.map(region => <option key={region} value={region}>{region}</option>)}</select></label></div></div><div className="mt-4 flex flex-wrap justify-end gap-2"><Button size="sm" variant="outline" onClick={exportCsv}><FileSpreadsheet className="mr-2 size-4" />Exportar CSV</Button><Button size="sm" variant="outline" onClick={exportPdf}><Download className="mr-2 size-4" />Exportar PDF</Button></div><div className="mt-5 grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><Metric label="Voluntários" value={data.summary.volunteers} description="No recorte selecionado" /><Metric label="Taxa de conclusão" value={`${data.summary.completionRate}%`} description={`${data.summary.completedVolunteers} com a trilha completa`} /><Metric label="Materiais ativos" value={data.summary.totalMaterials} description="Etapas da trilha atual" /><Metric label="Com vencimento" value={data.summary.overdueVolunteers} description="Com pelo menos um prazo atrasado" /></div><div className="mt-6 grid gap-5 xl:grid-cols-[.9fr_1.1fr]"><div><h3 className="flex items-center gap-2 font-semibold"><UsersRound className="size-4 text-primary" />Desempenho por região</h3><div className="mt-3 space-y-2">{data.byRegion.map(item => <div key={item.region} className="rounded-lg border p-3"><div className="flex items-center justify-between gap-3"><span className="text-sm font-medium">{item.region}</span><Badge variant="outline">{item.completionRate}%</Badge></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-muted"><div className="h-full bg-[#c9a85b]" style={{ width: `${item.completionRate}%` }} /></div><p className="mt-2 text-xs text-muted-foreground">{item.volunteers} voluntário(s) · {item.overdue} com prazo vencido</p></div>)}{!data.byRegion.length && <p className="rounded-lg bg-muted/50 p-4 text-sm text-muted-foreground">Ainda não há voluntários no filtro selecionado.</p>}</div></div><div><h3 className="flex items-center gap-2 font-semibold"><TimerReset className="size-4 text-primary" />Acompanhamento por voluntário</h3><div className="mt-3 max-h-72 space-y-2 overflow-auto pr-1">{data.volunteers.map(item => <div key={item.id} className="rounded-lg border p-3"><div className="flex items-center justify-between gap-3"><div><p className="text-sm font-medium">{item.name}</p><p className="text-xs text-muted-foreground">{item.coordinatorName || "Sem equipe responsável"}{item.region ? ` · ${item.region}` : ""}</p></div><Badge variant={item.overdue ? "destructive" : item.progress === 100 ? "default" : "secondary"}>{item.overdue ? `${item.overdue} vencido(s)` : `${item.progress}%`}</Badge></div><div className="mt-2 h-1.5 overflow-hidden rounded-full bg-muted"><div className="h-full bg-[#103527]" style={{ width: `${item.progress}%` }} /></div><p className="mt-2 text-xs text-muted-foreground">{item.completed}/{item.total} materiais concluídos</p></div>)}{!data.volunteers.length && <p className="rounded-lg bg-muted/50 p-4 text-sm text-muted-foreground">Nenhum voluntário encontrado.</p>}</div></div></div></section>;
}
