const prompt = `Revise, como consultor de produto e sem oferecer aconselhamento jurídico, a próxima prioridade para este módulo financeiro de campanha: ele já tem cadastro de receitas/despesas, saldo, categorias, contraparte, status, documentos, revisão centralizada, anexos PDF, vínculos planejados de evento/fornecedor/centro de custo e regras internas configuráveis. O usuário pediu que valores deixem de aparecer na interface somente após terminar a etapa financeira. Escolha uma única próxima entrega de maior impacto entre: (A) estados detalhados e alertas internos; (B) relatórios filtráveis e CSV/PDF; (C) orçamento por categoria; (D) busca avançada. Responda em JSON com {prioridade, justificativa, fluxo_minimo, risco_mitigado}.`;
const response = await fetch(`https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent?key=${process.env.GEMINI_API_KEY}`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ contents: [{ parts: [{ text: prompt }] }], generationConfig: { responseMimeType: "application/json" } }) });
if (!response.ok) throw new Error(`Gemini respondeu ${response.status}: ${await response.text()}`);
const payload = await response.json();
process.stdout.write(payload.candidates?.[0]?.content?.parts?.[0]?.text ?? "{}");
